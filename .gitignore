# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so
*.pyc
*.idea

# Python environments
.venv/
.venv*/
.vent/

# Logs and databases #
######################
*.log

# OS generated files #
######################
.DS_Store*
ehthumbs.db
Icon?
Thumbs.db

# --- Runtime / generated (VM + local; avoids git pull conflicts) ---
# Regenerate via ingestion / pipeline. If these were ever committed, run once:
#   git rm -r --cached .state json/*_latest.json data/raw ...   (adjust to match)
.state/
logs/
data/raw/
json/history/
json/*_latest.json
json/ingestion_summary_*.json
json/storage_parity_latest.json

# SQLite (dual-write / primary storage experiments)
*.sqlite
*.db

# Claude / local tooling (optional)
.claude/
